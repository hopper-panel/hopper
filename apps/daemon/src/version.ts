/**
 * Version du daemon, remplacée à la publication par le pipeline de release.
 * Elle est comparée à celle du panel : un écart de version majeure est signalé
 * à l'opérateur, car le contrat partagé peut avoir changé.
 */
export const DAEMON_VERSION = '0.0.0-dev';
